apiVersion: v1
kind: Service
metadata:
  name: api-gateway-service
  labels:
    app: ebmeds
    name: api-gateway-service
spec:
  type: LoadBalancer
  ports:
    - port: 3001
      targetPort: 3001
      nodePort: 30001
  selector:
    app: ebmeds
    name: api-gateway
